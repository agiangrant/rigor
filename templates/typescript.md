## TypeScript

When writing or modifying TypeScript code, use `/rigor:ts-writer` and follow all instructions.
When writing unit tests, use `/rigor:ts-unit-test` and follow all instructions.
When organizing code or restructuring, use `/rigor:ts-organizer` and follow all instructions.

All TypeScript skills enforce `/rigor:tdd` — tests come first, always.
